# Install production deps first
-r requirements.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0   # async test support
pytest-cov==6.0.0        # code coverage reports
httpx==0.27.2            # async HTTP client + ASGI test transport

# SQLite async support for tests (no PostgreSQL needed locally)
aiosqlite==0.20.0
greenlet==3.1.1          # Required by SQLAlchemy async for all DB operations

# ── Code Quality ─────────────────────────────────────────────────────────────
ruff==0.8.0              # fast linter + formatter (replaces flake8 + black)

# ── Type Checking ────────────────────────────────────────────────────────────
mypy==1.13.0
